
// There is no namespacing in ES5, every global thing "pollutes" the global namespace,
// and is shared between all scripts loaded by the HTML document
var myGlobalVariable = 'hello I am global variable';
